# Kore OS AppArmor configuration
# AppArmor is enabled at boot via kernel parameters

# Cache location
cache-loc=/var/cache/apparmor
cache-locs=/var/cache/apparmor

# Include standard policy
include /etc/apparmor.d/
